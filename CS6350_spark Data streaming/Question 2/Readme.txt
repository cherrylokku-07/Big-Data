1. First, create a cluster and add the graphframes module (match the version) to this cluster by adding it in libraries tab in cluster configuration.
2. Attach this cluster to our published notebook
3. Simply run the notebook to see all the results for queries